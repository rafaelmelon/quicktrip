/* jshint esversion:6 */

// const User = require('../models/user-model');
//
// function mapChecker(req, res, next){
//     User.findById(req.user._id, (error, user) => {
//         if (error) { return next(error); }
//
//         if (user.usermap === null) {
//             return next();
//         } else {
//             return res.redirect('/');
//         }
//     });
// }
//
// module.exports = mapChecker;
